# Environment variables for production

# Google Cloud Vision API (Optional but recommended for better OCR)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# Or base64 encoded credentials for Vercel
# GOOGLE_CREDENTIALS_BASE64=your_base64_encoded_credentials

# Tesseract OCR path (if using local Tesseract)
# TESSERACT_CMD=/usr/bin/tesseract

# API Configuration
# MAX_FILE_SIZE=10485760  # 10MB in bytes
